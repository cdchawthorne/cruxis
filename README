A basic CLI network manager.
Implemented in python3.

Dependencies:
python3
python-dbus
python-gobject2
bash

To run the daemon on startup, run the following as root:
systemctl enable cruxis-daemon.service
