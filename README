A basic CLI network manager.
Implemented in erlang.

To run the daemon on startup, add cruxis-daemon to the DAEMONS array in
/etc/rc.conf:
DAEMONS=(... cruxis-daemon)

To connect to known networks on startup, add the following line to /etc/rc.local:
cruxis auto_connect &
